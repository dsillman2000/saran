# TL-08: Valid minimal wrapper with all required fields
# Expected: Validation success

name: "test-wrapper"
version: "1.0.0"
commands:
  test:
    actions:
      - executable: "echo"
        args: []
